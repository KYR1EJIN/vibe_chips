{
  "name": "vibe-chips",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=shared && npm run build --workspace=server && npm run build --workspace=client",
    "start": "npm run start --workspace=server",
    "lint": "npm run lint --workspace=client && npm run lint --workspace=server && npm run lint --workspace=shared",
    "type-check": "npm run type-check --workspace=client && npm run type-check --workspace=server && npm run type-check --workspace=shared"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

